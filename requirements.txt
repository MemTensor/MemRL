# Single entrypoint for setting up an environment that can run all 4 benchmarks.
#
# Usage:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt
#
# Notes:
# - Installs MemRL in editable mode.
# - Benchmark-specific deps live under `requirements/`.

-e .
-r requirements/benchmarks.txt
